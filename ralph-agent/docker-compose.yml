version: '3.8'

services:
  ralph-workspace:
    image: alpine:latest
    container_name: ralph-workspace
    volumes:
      - ./workspace:/app
    working_dir: /app
    command: tail -f /dev/null
    restart: always
